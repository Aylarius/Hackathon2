<!DOCTYPE html>
<html>
<!--*************** HEAD ***************-->
<head>
    <meta charset="utf-8"/>
    <title>{% block title %}Movember{% endblock %}</title>
    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css"/>
    <link rel="stylesheet" href="{{ asset('bundles/mov/css/style.css') }}"/>
</head>
<body>

<header>
    <nav class="navbar navbar-default navbar-fixed-top">
        <div class="container-fluid" id="container-nav">
            <ul class="nav navbar-nav center-block">
                <li><a href="#">
                        <h1>Movember</h1>
                        <img src="{{ asset('bundles/mov/img/mustache.png') }}" alt="" width="70px" />
                    </a>
                </li>
            </ul>
        </div><!-- /.container-fluid -->
    </nav>
</header>

<main class="container-fluid">
    <div class="row">
        <div class="col-xs-12">
            {% block body %}

                {% trans_default_domain 'FOSUserBundle' %}

                {% if error %}
                    <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                {% endif %}
                <div class="row" style="text-align: center;">

                    <form action="{{ path("fos_user_security_check") }}" method="post">
                        {% if csrf_token %}
                            <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
                        {% endif %}
                        <div class="form-group">
                            <label for="username">{{ 'security.login.username'|trans }}</label>
                            <input type="text" id="username" name="_username" value="{{ last_username }}" required="required" />
                        </div><br>
                        <div class="form-group">
                            <label for="password">{{ 'security.login.password'|trans }}</label>
                            <input type="password" id="password" name="_password" required="required" />
                        </div><br>
                        <div class="form-group">
                            <input type="checkbox" id="remember_me" name="_remember_me" value="on" />
                            <label for="remember_me">{{ 'security.login.remember_me'|trans }}</label>
                        </div><br>
                        <input type="submit" id="_submit" name="_submit" value="{{ 'security.login.submit'|trans }}" />
                    </form>
                </div>
                <br>
                <div class="social">
                    <button type="button" class="facebook-button"><a href="{{ path('hwi_oauth_service_redirect', {'service': 'facebook' }) }}">Facebook</a></button>
                    <button type="button" class="google-button"><a href="{{ path('hwi_oauth_service_redirect', {'service': 'google' }) }}">Google</a></button>
                    <button type="button" class="twitter-button"><a href="{{ path('hwi_oauth_service_redirect', {'service': 'twitter' }) }}">Twitter</a></button>
                </div>
                <a href="{{ path('fos_user_registration_register') }}">S'inscrire</a>
            {% endblock %}
        </div>
    </div>
</main>

</body>

<!--********** FOOTER **********-->
<footer>
    <p class="container">Copyright© 2016</p>
</footer>
<!--************* SCRIPTS ***************-->
<script src="https://use.fontawesome.com/f0f4368368.js"></script>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>

</html>